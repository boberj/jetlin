{
  "name": "jetlin-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end tests driving the counter sample in a real browser.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
